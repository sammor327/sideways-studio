node_modules/
data/
*.log
# PSD composite used only by a scene's ?debug=psd placement check.
_psd-reference.png
build/
dist/
# The card-store secret baked into released builds. Never commit it, and
# never lose it: see scripts/build-exe.mjs and server/cardstore.js.
.carddb-key
